<%= form_for(@way) do |f| %>
  <% if @way.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@way.errors.count, "error") %> prohibited this way from being saved:</h2>

      <ul>
      <% @way.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <%= f.label "Language: " %>
  <%= f.collection_select :language_id, Language.all, :id, :name, class: "form-control" %>
  <%= f.label :category %>
  <%= f.select :category, ['book','video','mooc', 'tutorial', 'bootcamp', 'blog post', 'game', 'university'] %>
  <div class="actions">

    <%= f.submit %>
  </div>
<% end %>
