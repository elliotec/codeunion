<h1>Resources</h1>
  <% @resources.each do |resource| %>
    <h2><%= link_to resource.name, resource %></h2>
  <% end %>

<% if can? :update, @resource %>
  <%= link_to 'New Resource', new_resource_path %>
<% end %>
